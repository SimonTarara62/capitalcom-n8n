# Capital.com DEMO credentials for local integration tests: `npm run test:integration`.
# Copy this file to `.env` (which is gitignored) and fill in real DEMO keys.
# NEVER put live keys here and NEVER commit a real .env.
CAP_API_KEY=
CAP_IDENTIFIER=
CAP_API_PASSWORD=
