// n8n discovers nodes & credentials via the "n8n" block in package.json.
// This module intentionally exports nothing; it exists so the TypeScript
// build always has a compilation entry point.
export {};
